{
  "name": "unityjs",
  "version": "0.0.2",
  "type": "module",
  "devDependencies": {
    "rollup": "^4.59.0"
  },
  "scripts": {
    "build": "rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist"
  ]
}
